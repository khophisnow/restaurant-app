import { RestaurantSettingsEntity } from './restaurant_settings.entity';

describe('RestaurantSettingsEntity', () => {
  it('should be defined', () => {
    expect(new RestaurantSettingsEntity()).toBeDefined();
  });
});
