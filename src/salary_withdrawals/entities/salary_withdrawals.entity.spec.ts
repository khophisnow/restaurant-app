import { SalaryWithdrawalsEntity } from './salary_withdrawals.entity';

describe('SalaryWithdrawalsEntity', () => {
  it('should be defined', () => {
    expect(new SalaryWithdrawalsEntity()).toBeDefined();
  });
});
